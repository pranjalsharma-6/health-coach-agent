# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Data validation / settings ---
pydantic==2.10.5
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database (async driver) ---
motor==3.6.0
pymongo==4.9.2
dnspython==2.7.0

# --- Auth ---
PyJWT==2.10.1
bcrypt==4.2.1

# --- Agent / LLM ---
langchain-core==0.3.29
langchain-groq==0.2.3
langchain-openai==0.2.14
# 2.0.7 is the newest release that works with langchain-core 0.3.29, which
# langchain-groq and langgraph pin. Later versions require langchain-core
# 1.x and break both. It warns that Google's older SDK is deprecated; it
# still works, and upgrading the whole LangChain stack is a larger change.
langchain-google-genai==2.0.7
langgraph==0.2.61

# --- Utilities ---
python-dotenv==1.0.1
httpx==0.27.2
